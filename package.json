{
  "name": "gemini-inline",
  "version": "2.0.0",
  "description": "A CLI tool to collaborate with Gemini on local files via inline comments (Daemon Mode).",
  "main": "dist/index.js",
  "bin": {
    "gemini-inline": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  }
}
